{
  "name": "turbotags",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "vercel dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "@google/generative-ai": "^0.24.1"
  },
  "engines": {
    "node": ">=18"
  }
}
